# Binary Search

Uses binary search on a sorted integer list to quickly find a target.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o binary_search main.c
```

## Run

```bash
./binary_search
```
