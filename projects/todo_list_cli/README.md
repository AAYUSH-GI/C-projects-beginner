# To-Do List CLI

A tiny interactive to-do list that stores tasks in memory.

Commands:
- `a` add a task
- `l` list tasks
- `r` remove a task by number
- `q` quit

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o todo_list_cli main.c
```

## Run

```bash
./todo_list_cli
```
