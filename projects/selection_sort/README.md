# Selection Sort

Demonstrates selection sort, repeatedly selecting the smallest remaining element.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o selection_sort main.c
```

## Run

```bash
./selection_sort
```
