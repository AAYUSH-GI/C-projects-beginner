# Hello World

A minimal starting point that prints "Hello, world!" to the terminal.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o hello_world main.c
```

## Run

```bash
./hello_world
```
