# Array Stats

Reads a series of integers from the user, then prints their sum and average.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o array_stats main.c
```

## Run

```bash
./array_stats
```
