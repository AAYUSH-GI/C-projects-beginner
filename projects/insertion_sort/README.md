# Insertion Sort

Uses insertion sort to place each element into its correct position in a growing sorted list.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o insertion_sort main.c
```

## Run

```bash
./insertion_sort
```
