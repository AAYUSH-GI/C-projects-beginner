# Palindrome Checker

Ignores spaces and capitalization to decide whether the provided text is a palindrome.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o palindrome_checker main.c
```

## Run

```bash
./palindrome_checker
```
