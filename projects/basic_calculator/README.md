# Basic Calculator

A command-line calculator that performs +, -, *, and / on two numbers.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o basic_calculator main.c
```

## Run

```bash
./basic_calculator
```

Enter input in the form `number operator number`, such as `3.5 * 2`.
