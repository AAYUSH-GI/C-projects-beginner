# Linear Search

Walks through an array one element at a time to find a target value.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o linear_search main.c
```

## Run

```bash
./linear_search
```
