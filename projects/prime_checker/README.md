# Prime Checker

Determines whether an integer is prime using trial division up to the square root.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -lm -o prime_checker main.c
```

## Run

```bash
./prime_checker
```
