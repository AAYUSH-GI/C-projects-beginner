# Temperature Converter

Convert temperatures between Celsius and Fahrenheit.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o temperature_converter main.c
```

## Run

```bash
./temperature_converter
```

Enter an input like `100 C` or `32 F` to see the converted value.
