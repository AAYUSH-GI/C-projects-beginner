# Character Counter

Counts characters, words, and lines from standard input.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o character_counter main.c
```

## Run

```bash
./character_counter
```
