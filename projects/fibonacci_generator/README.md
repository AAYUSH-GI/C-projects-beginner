# Fibonacci Generator

Prints the Fibonacci sequence up to a user-defined count.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o fibonacci_generator main.c
```

## Run

```bash
./fibonacci_generator
```
