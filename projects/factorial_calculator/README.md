# Factorial Calculator

Computes factorials of non-negative integers using a simple loop.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o factorial_calculator main.c
```

## Run

```bash
./factorial_calculator
```
