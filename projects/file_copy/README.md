# File Copy

Copies a source file to a destination using buffered reads and writes.

## Build

```bash
gcc -std=c11 -Wall -Wextra -Wpedantic -o file_copy main.c
```

## Run

```bash
./file_copy source.txt destination.txt
```
